/*import { StoreState } from "../types/index";
import { SHUFFLE, CHANGE_FONT } from './../constants/index';
import { ContainerAction } from "../actions/index";

export function reducer(state: StoreState, action: ContainerAction): StoreState {
    switch (action.type){
        case SHUFFLE:
            return { ...state, text: action.payload};
        case CHANGE_FONT:
            return { ...state, text: action.payload};
        default:
            return state;
    }
}
*/